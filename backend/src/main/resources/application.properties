# App
spring.application.name=AI_Business_Backend

# Server
server.port=8080
server.address=0.0.0.0

# --- Local MySQL Database ---
spring.datasource.url=jdbc:mysql://localhost:3306/ai_busines_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=23Ug032546$
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# No need to set dialect explicitly; Hibernate auto-detects MySQL
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Security (local only)
spring.security.user.name=admin
spring.security.user.password=admin123

# Logging
logging.level.com.zaxxer.hikari=DEBUG

logging.level.org.springframework.security=DEBUG

